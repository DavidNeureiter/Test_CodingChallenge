# Test_CodingChallenge
The information will be stored in a .json file, which is created when the application starts.
The .json file is located in the temporary path of the current user's temporary folder.
